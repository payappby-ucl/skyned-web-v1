import { DepartmentController, departmentController } from ".";

describe("DepartmentController", () => {
  test("should be instance of DepartmentController", () => {
    expect(departmentController).toBeInstanceOf(DepartmentController);
  });
});
