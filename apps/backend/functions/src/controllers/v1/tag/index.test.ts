import { tagController, TagController } from ".";

describe("tagController", () => {
  describe("Instance", () => {
    test("should be an instance of TagController", () => {
      expect(tagController).toBeInstanceOf(TagController);
    });
  });
});
