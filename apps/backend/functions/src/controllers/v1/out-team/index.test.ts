import { ourTeamController, OurTeamController } from ".";

describe("OurTeamController", () => {
  describe("instance", () => {
    test("should be an instance of OurTeamController", () => {
      expect(ourTeamController).toBeInstanceOf(OurTeamController);
    });
  });
});
