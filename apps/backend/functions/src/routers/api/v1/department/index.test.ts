import { DepartmentRouter, departmentRouter } from ".";

describe("DepartmentRouter", () => {
  test("should be an instance of DepartmentRouter", () => {
    expect(departmentRouter).toBeInstanceOf(DepartmentRouter);
  });
});
