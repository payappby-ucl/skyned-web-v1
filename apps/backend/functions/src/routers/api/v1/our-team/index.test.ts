import { ourTeamRouter, OurTeamRouter } from ".";

describe("OurTeamRouter", () => {
  test("should be an instance of OurTeamRouter", () => {
    expect(ourTeamRouter).toBeInstanceOf(OurTeamRouter);
  });
});
