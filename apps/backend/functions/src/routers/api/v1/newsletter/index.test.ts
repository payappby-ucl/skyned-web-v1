import { NewsletterRouter, newsletterRouter } from ".";

describe("NewsletterRouter", () => {
  test("should be an instance of NewsletterRouter", () => {
    expect(newsletterRouter).toBeInstanceOf(NewsletterRouter);
  });
});
