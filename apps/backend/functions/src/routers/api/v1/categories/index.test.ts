import { categoryRouter, CategoryRouter } from ".";

describe("categoryRouter", () => {
  test("should be an instance of CategoryRouter", () => {
    expect(categoryRouter).toBeInstanceOf(CategoryRouter);
  });
});
