import { schoolRouter, SchoolRouter } from ".";

describe("SchoolRouter", () => {
  test("should be an instance of SchoolRouter", () => {
    expect(schoolRouter).toBeInstanceOf(SchoolRouter);
  });
});
