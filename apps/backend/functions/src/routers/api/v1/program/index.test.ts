import { programRouter, ProgramRouter } from ".";

describe("ProgramRouter", () => {
  test("should be an instance of ProgramRouter", () => {
    expect(programRouter).toBeInstanceOf(ProgramRouter);
  });
});
