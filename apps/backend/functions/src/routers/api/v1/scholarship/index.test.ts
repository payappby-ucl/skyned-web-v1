import { scholarshipRouter, ScholarshipRouter } from ".";

describe("ScholarshipRouter", () => {
  test("should be an instance of ScholarshipRouter", () => {
    expect(scholarshipRouter).toBeInstanceOf(ScholarshipRouter);
  });
});
