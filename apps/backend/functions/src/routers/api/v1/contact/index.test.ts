import { contactRouter, ContactRouter } from ".";

describe("ContactRouter", () => {
  test("should be an instance of ContactRouter", () => {
    expect(contactRouter).toBeInstanceOf(ContactRouter);
  });
});
