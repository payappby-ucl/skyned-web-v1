components:
  parameters:
    Slug:
      name: slug
      in: path
      description: School identifier
      required: true
      schema:
        type: string
    ProgramSlug:
      name: slug
      in: path
      description: School identifier
      required: true
      schema:
        type: string
