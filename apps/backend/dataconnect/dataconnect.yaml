specVersion: "v1beta"
serviceId: "backend"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "skyned"
      cloudSql:
        instanceId: "backend-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
